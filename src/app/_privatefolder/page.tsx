const Privatefolder = () => {
    return ( 
        <div>
            <h1>Private Folder</h1>
        </div>
     );
}
 
export default Privatefolder;

// if we save a file with its name starting with underscore, the file is now private so you cannot access it directly from the browser.